# Milestones and the tree

One section per milestone. Copy the block below. Point at folders in this repository, not at a finished game's file list.

## Milestone

Name: short name

Done when: the sentence that means this milestone is finished

Folders: the directories that hold that work (`assets/`, `animations/`, `source/`, `addons/`, `tests/`, or `tools/`, and the subfolders that apply)
